import { useState } from 'react';
import { ArrowRight, Check, FileCheck2, LoaderCircle, MapPin, ShieldCheck } from 'lucide-react';
import { Link, useLocation } from 'wouter';
import type { FormEvent, ReactNode } from 'react';
import { useCreatePartner } from '@workspace/api-client-react';
import { BackLink, Brand, SectionLabel } from '@/components/source-ui';

const initialForm = { businessName: '', contactName: '', abn: '', trade: 'Plumbing', licence: '', mobile: '', email: '', suburbs: '', radiusKm: '15', emergencyJobs: false, services: '' };

export default function PartnerPage() {
  const [, setLocation] = useLocation();
  const [form, setForm] = useState(initialForm);
  const [submitted, setSubmitted] = useState(false);
  const [error, setError] = useState('');
  const createPartner = useCreatePartner();
  const update = (key: keyof typeof initialForm, value: string | boolean) => setForm((current) => ({ ...current, [key]: value }));
  const submit = (event: FormEvent) => {
    event.preventDefault();
    setError('');
    if (!form.businessName || !form.contactName || !form.mobile || !form.email || !form.suburbs) { setError('Please complete the required fields so we can review your application.'); return; }
    createPartner.mutate({ data: { businessName: form.businessName, contactName: form.contactName, abn: form.abn, trade: form.trade, licence: form.licence, mobile: form.mobile, email: form.email, suburbs: form.suburbs.split(',').map((item) => item.trim()).filter(Boolean), radiusKm: Number(form.radiusKm) || 15, emergencyJobs: form.emergencyJobs, services: form.services.split(',').map((item) => item.trim()).filter(Boolean) } }, { onSuccess: () => setSubmitted(true), onError: () => setError('That application could not be sent. Please try again.') });
  };
  if (submitted) return <SuccessScreen onDashboard={() => setLocation('/partner/dashboard')} />;
  return <div className="min-h-[100dvh]"><header className="border-b border-[hsl(var(--border))]"><div className="content-wrap flex min-h-[76px] items-center justify-between"><Brand /><Link href="/" className="btn-quiet text-sm" data-testid="link-partner-home">Back home</Link></div></header><main className="content-wrap grid max-w-[1100px] gap-10 py-10 pb-20 md:grid-cols-[.7fr_1.3fr] md:py-16"><div className="md:sticky md:top-10 md:self-start"><BackLink href="/">Home</BackLink><SectionLabel>Partner network</SectionLabel><h1 className="mt-3 text-5xl font-bold leading-[.9] tracking-[-.075em] md:text-7xl">Work that<br /><span className="font-display font-normal italic text-[hsl(var(--secondary))]">fits.</span></h1><p className="mt-7 max-w-sm text-base leading-7 text-[hsl(var(--muted-foreground))]">SourceTradie brings the right local jobs to good operators. You decide when you’re available and what’s worth your time.</p><div className="mt-10 space-y-4"><Promise icon={<MapPin size={17} />} title="Stay in your patch" detail="Set your suburbs and travel radius." /><Promise icon={<ShieldCheck size={17} />} title="Better briefings" detail="See the customer’s real words before deciding." /><Promise icon={<FileCheck2 size={17} />} title="No lock-in" detail="Apply once. Keep control of your diary." /></div></div><form onSubmit={submit} className="glass-card rounded-[1.5rem] p-5 md:p-8"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] pb-5"><div><SectionLabel>Expression of interest</SectionLabel><h2 className="mt-2 text-2xl font-bold tracking-[-.05em]">Tell us about the business.</h2></div><span className="hidden rounded-full bg-[hsl(var(--muted))] px-3 py-1 font-mono-ui text-[9px] uppercase tracking-[.12em] sm:inline-flex">Demo application</span></div><div className="mt-7 space-y-6"><div className="grid gap-5 sm:grid-cols-2"><Field label="Business name *" id="businessName" value={form.businessName} onChange={(value) => update('businessName', value)} placeholder="Northside Flow" /><Field label="Your name *" id="contactName" value={form.contactName} onChange={(value) => update('contactName', value)} placeholder="Mia Chen" /></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Trade *" id="trade" value={form.trade} onChange={(value) => update('trade', value)} type="select" options={['Plumbing', 'Electrical', 'Heating & cooling', 'Carpentry', 'General maintenance']} /><Field label="ABN" id="abn" value={form.abn} onChange={(value) => update('abn', value)} placeholder="12 345 678 901" /></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Mobile *" id="mobile" value={form.mobile} onChange={(value) => update('mobile', value)} placeholder="04xx xxx xxx" /><Field label="Email *" id="email" value={form.email} onChange={(value) => update('email', value)} placeholder="hello@northsideflow.com.au" type="email" /></div><div className="grid gap-5 sm:grid-cols-[1.4fr_.6fr]"><Field label="Suburbs you cover *" id="suburbs" value={form.suburbs} onChange={(value) => update('suburbs', value)} placeholder="Brunswick, Coburg, Preston" /><Field label="Radius (km)" id="radiusKm" value={form.radiusKm} onChange={(value) => update('radiusKm', value)} placeholder="15" type="number" /></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Licence or registration" id="licence" value={form.licence} onChange={(value) => update('licence', value)} placeholder="REC / VBA number" /><Field label="Services" id="services" value={form.services} onChange={(value) => update('services', value)} placeholder="Leaks, hot water, repairs" /></div><label className="flex cursor-pointer items-start gap-3 rounded-xl bg-[hsl(var(--muted)/.65)] p-4 text-sm leading-6"><input type="checkbox" checked={form.emergencyJobs} onChange={(event) => update('emergencyJobs', event.target.checked)} className="mt-1 h-4 w-4 accent-[hsl(var(--secondary))]" data-testid="checkbox-emergency-jobs" /><span><strong>Open to emergency jobs</strong><br /><span className="text-[hsl(var(--muted-foreground))]">Only choose this if you can safely respond outside regular hours.</span></span></label></div>{error && <p className="mt-5 rounded-xl bg-[hsl(var(--destructive)/.08)] p-3 text-sm text-[hsl(var(--destructive))]" data-testid="error-partner">{error}</p>}<div className="mt-7 flex flex-col gap-3 border-t border-[hsl(var(--border))] pt-6 sm:flex-row sm:items-center sm:justify-between"><p className="text-xs text-[hsl(var(--muted-foreground))]">We’ll review your details before approval.</p><button className="btn-accent" type="submit" disabled={createPartner.isPending} data-testid="button-submit-partner">{createPartner.isPending ? <LoaderCircle size={16} className="animate-spin" /> : <ArrowRight size={16} />} {createPartner.isPending ? 'Sending application' : 'Apply to join'}</button></div></form></main></div>;
}

function Field({ label, id, value, onChange, placeholder, type = 'text', options }: { label: string; id: string; value: string; onChange: (value: string) => void; placeholder?: string; type?: string; options?: string[] }) {
  return <div><label className="label" htmlFor={id}>{label}</label>{type === 'select' ? <select id={id} className="field" value={value} onChange={(event) => onChange(event.target.value)} data-testid={`select-${id}`}>{options?.map((option) => <option value={option} key={option}>{option}</option>)}</select> : <input id={id} className="field" type={type} value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} data-testid={`input-${id}`} />}</div>;
}
function Promise({ icon, title, detail }: { icon: ReactNode; title: string; detail: string }) {
  return <div className="flex gap-3"><span className="mt-0.5 text-[hsl(var(--secondary))]">{icon}</span><div><p className="text-sm font-semibold">{title}</p><p className="mt-0.5 text-xs leading-5 text-[hsl(var(--muted-foreground))]">{detail}</p></div></div>;
}
function SuccessScreen({ onDashboard }: { onDashboard: () => void }) {
  return <div className="min-h-[100dvh]"><header className="border-b border-[hsl(var(--border))]"><div className="content-wrap flex min-h-[76px] items-center"><Brand /></div></header><main className="content-wrap grid min-h-[calc(100dvh-77px)] max-w-[720px] place-items-center py-16 text-center"><div className="animate-rise"><div className="mx-auto grid h-16 w-16 place-items-center rounded-[1.25rem] bg-[hsl(var(--secondary))] text-[hsl(var(--secondary-foreground))]"><Check size={30} /></div><SectionLabel>Application received</SectionLabel><h1 className="mt-3 text-5xl font-bold leading-[.9] tracking-[-.075em] md:text-7xl">We’ll take a<br /><span className="font-display font-normal italic">good look.</span></h1><p className="mx-auto mt-7 max-w-md text-base leading-7 text-[hsl(var(--muted-foreground))]">Your details are in the review queue. This demo does not approve partners automatically.</p><button className="btn-main mt-8" onClick={onDashboard} data-testid="button-view-partner-dashboard">View partner dashboard <ArrowRight size={16} /></button></div></main></div>;
}